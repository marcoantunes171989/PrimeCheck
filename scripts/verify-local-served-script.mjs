import assert from 'node:assert/strict'
import { readFileSync } from 'node:fs'

const validator = readFileSync(new URL('../validar-local.cmd', import.meta.url), 'utf8')
const launcher = readFileSync(new URL('./primecheck-local.ps1', import.meta.url), 'utf8')
const updater = readFileSync(new URL('../atualizar-validar-local.cmd', import.meta.url), 'utf8')
const updaterRunner = readFileSync(new URL('./atualizar-validar-local-runner.cmd', import.meta.url), 'utf8')
const verifier = readFileSync(new URL('./verify-local-served-build.mjs', import.meta.url), 'utf8')
const vite = readFileSync(new URL('../vite.config.ts', import.meta.url), 'utf8')
const main = readFileSync(new URL('../src/main.tsx', import.meta.url), 'utf8')
const index = readFileSync(new URL('../index.html', import.meta.url), 'utf8')

assert.match(updaterRunner, /git reset --hard origin\/homologacao-local-validacao/)
assert.match(updaterRunner, /marcoantunes171989\/PrimeCheck/)
assert.match(updaterRunner, /\[7\/8\] Confirmando tela Consulta de produtos/)

assert.match(validator, /powershell -NoProfile -ExecutionPolicy Bypass -File/)
assert.match(validator, /primecheck-local\.ps1/)
assert.doesNotMatch(validator, /-ProjectDir/)
assert.doesNotMatch(validator, /npm run serve:local/)
assert.doesNotMatch(validator, /start-local-server\.mjs|vite-local-daemon\.mjs/)

assert.match(launcher, /\$Port = 4177/)
assert.match(launcher, /Trim\(\)\.Trim\('"')\.TrimEnd\('\\\\')/)
assert.match(launcher, /Resolve-Path -LiteralPath/)
assert.match(launcher, /Get-NetTCPConnection -LocalPort \$Port -State Listen/)
assert.match(launcher, /node_modules\\vite\\bin\\vite\.js/)
assert.match(launcher, /Start-Process -FilePath \$NodeCmd/)
assert.match(launcher, /--host", "127\.0\.0\.1", "--port", "\$Port", "--strictPort"/)
assert.match(launcher, /Invoke-WebRequest -UseBasicParsing -Uri \$url/)
assert.match(launcher, /<title>PrimeCheck \\| Homologa/)
assert.match(launcher, /Pedido Prime/)
assert.match(launcher, /Pesquisa por produtos/)
assert.match(launcher, /Consulta de produtos/)
assert.match(launcher, /Processamento local/)
assert.match(launcher, /data-primecheck-runtime-error/)
assert.match(launcher, /PRIMECHECK_CI/)
assert.match(launcher, /foregroundArgs/)
assert.match(launcher, /& \$NodeCmd @foregroundArgs/)
assert.match(launcher, /--open/)
assert.match(launcher, /Iniciando Vite definitivo/)
assert.match(launcher, /Servidor definitivo em primeiro plano/)

assert.match(verifier, /Pesquisa por produtos/)
assert.match(verifier, /Consulta de produtos/)
assert.match(verifier, /expectedSha\.slice\(0, 12\)/)

assert.match(main, /PrimeCheckRuntimeBoundary/)
assert.match(main, /data-primecheck-runtime-error/)
assert.match(index, /primecheck-local-cleanup/)
assert.match(index, /<title>PrimeCheck \| Homologação de Dados<\/title>/)
assert.doesNotMatch(index, /Pedido Prime/)

assert.match(updater, /RUNNER_TEMP/)
assert.match(updater, /copy \/Y "%RUNNER_SOURCE%" "%RUNNER_TEMP%"/)
assert.match(updater, /call "%RUNNER_TEMP%" "%PROJECT_DIR%"/)
assert.doesNotMatch(updater, /git fetch|git reset|git switch|git checkout/)
assert.match(updaterRunner, /git checkout -B homologacao-local-validacao origin\/homologacao-local-validacao/)
assert.match(updaterRunner, /git reset --hard origin\/homologacao-local-validacao/)
assert.doesNotMatch(updaterRunner, /git switch -c/)
assert.doesNotMatch(updaterRunner, /git show-ref --verify/)

assert.match(vite, /Cache-Control': 'no-store, no-cache, must-revalidate, proxy-revalidate'/)

console.log('PrimeCheck exact PowerShell local launcher verification: OK')
